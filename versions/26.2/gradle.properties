# Per-node dependency versions for Minecraft 26.2.
# Read by the shared build.gradle.kts; a missing entry fails the build loudly.
fabric_api_version=0.158.0+26.2

# Optional config-GUI libraries. Both publish one artifact per Minecraft version,
# so the coordinate differs per node even though the source code does not.
yacl_version=3.9.6+26.2-fabric
modmenu_version=20.0.1
